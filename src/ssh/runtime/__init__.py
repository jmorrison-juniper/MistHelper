"""SSH runtime orchestration package (T013d).

Concrete decomposed implementations for the SSH runner's top-level CLI
entrypoint and interactive REPL. Callers MUST import the concrete classes
(AppRunner, InteractiveMode) from their submodules directly. This package
contains no re-export shims and no façade indirection.
"""
