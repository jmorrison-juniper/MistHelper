"""Slug category and content sub-category classification.

This sub-package matches each slug to a category, reads a bounded PDF text
sample in memory, and derives a content sub-category label from the signals.
"""
