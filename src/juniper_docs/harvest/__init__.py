"""Durable state, manifest render, and run orchestration.

This sub-package holds the SQLite state store, the manifest writer, and the
runner that drives the whole harvest from discovery to the final summary.
"""
