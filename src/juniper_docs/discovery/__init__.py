"""Inventory discovery and the release-note filter.

This sub-package reads the sitemap index and each child sitemap, builds the
document inventory, reads each product landing page for its direct-PDF
references, and keeps only the newest release note in each train.
"""
