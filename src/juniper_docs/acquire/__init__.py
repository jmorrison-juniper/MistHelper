"""HTTP client, PDF resolution, and download.

This sub-package holds the shared HTTP configuration, the catalog client, the
companion PDF resolver, and the downloader. The reused base classes live here.
"""
