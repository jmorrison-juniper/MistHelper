"""Recorded fixtures for the harvester unit tests.

The fixtures hold a sitemap index, one child sitemap subset, two root index
pages, a table-of-contents script, a minimal valid PDF, and an HTML error page
served with a 200 status. No test performs a live network call.
"""
