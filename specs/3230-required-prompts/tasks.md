# Tasks: Required portal prompts

1. Add the issue #3230 registry block before `return registry`.
2. Build chooser options from the exporter source tables.
3. Add required text controls for MSP ID, SSID, and the email change token.
4. Mark menus 263 through 268 as command-line only.
5. Add `tests/unit/web_portal/test_portal_required_answers.py`.
6. Prove the guard fails when the registry repair is removed.
7. Run formatting, lint, unit test, and test-quality gates.
8. Verify menus 235, 238, 259, and 260 in a browser on port 9613.
9. Open the follow-up feature issue for dynamic endpoint family prompts.
10. Open the pull request.
