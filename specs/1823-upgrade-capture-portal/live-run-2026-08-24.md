# Task T232. The quickstart run against a real site

**Date**: 2026-08-24.
**Organization**: Morrison House, `8a1ea872-241a-4c8e-a5ca-2d85674c7229`.
**Site**: Morrison House Site, `cf36153a-97bb-4974-8f8f-e9cc25d64d83`.
**Devices**: 8. One SRX1500 gateway, one EX4100-F-12P switch, and six access
points.
**Driver**: a real browser, driven page by page. No test harness took part.

---

## 1. Why this record exists

`quickstart-results.md` recorded 26 scenarios on 2026-08-19 as 13 pass, 0 fail,
and 13 blocked. Every scenario that exercises the feature end to end sat in the
blocked half, because no run had ever reached a real organization. The audit of
2026-08-20 states the rule that the file states itself: a scenario that did not
run never counts as a pass.

This run reached a real organization. It found five defects that 2686 code tests
and 148 browser tests had all passed over. Section 3 names each one.

---

## 2. What the run proved

| Step | Result |
| --- | --- |
| The portal starts and answers `/healthz` | Pass |
| The sign-in page offers the token mode | Pass, after defect 3 |
| The token sign-in reaches the cloud | Pass, after defect 2 |
| The organization picker names Morrison House | Pass, after defect 1 |
| The site picker lists the sites and the lock state | Pass |
| The inventory page shows all 8 devices | Pass, after defects 4 and 5 |
| The site lock takes and releases from the page | Pass |
| A tier 2 capture reads the whole site | Pass |
| The capture stores a document | Fail. Section 4 holds the cause |

The capture read the real site in about 3 seconds. It found 8 devices, all 8
connected, and it counted 1 gateway, 1 switch, and 6 access points. It found 37
wired clients and 16 wireless clients. It wrote a backup file of 18,079 bytes at
`data/upgrade_capture_cap-1e12f0068bcd4a1b99ee6572e2328bd0-01.csv`, and that file
names `Morrison-Switch`, `SRX-1500`, `EX4100-F-12P`, the version `25.2R1-S2.3`,
and `Office AP`.

Warning: no device of this run received firmware. The run stopped at the capture,
which is the read half of the feature. T233 and the upgrade half stay open.

---

## 3. The five defects that only a real run could find

Every one of these passed the whole test suite. Four of the five share one cause:
a stand-in answered a simpler shape than the library or the module really
answers, so every reader passed its own test and failed the cloud.

| Defect | Where | What an operator met |
| --- | --- | --- |
| 1. The privilege list reads as absent | `runtime/identity.py session_privileges` tested `isinstance(value, list)`. `mistapi` 0.63 answers a `Privileges` object, which iterates and holds no length | The organization picker showed "This sign-in reaches no organization" for a token that reaches one. The journey stopped at the first page |
| 2. The token sign-in never signs in | `app/routes/auth.py default_token_session` built the session and returned it | The cloud never named the scope, so the picker stayed empty even after defect 1 |
| 3. The entry point reads no `.env` | `wsgi_capture.py` never loaded the file. `MistHelper.py` loads it, so menu 238 works and Gunicorn does not | The sign-in page said "This server holds no API token variable" while `.env` named one. The container runs `gunicorn wsgi_capture:app`, so the token mode was unavailable there too |
| 4. The inventory read drops the session | `app/routes/select.py read_inventory` called the device reader with two identifiers. The reader names `session` first | `GET /select/site/<id>` answered 500 for every real site |
| 5. The inventory answer reads as empty | `as_records` knew a list and a mapping. The device module answers a `DeviceRead` object that carries the rows under `records` | The page rendered and showed no device for a site that holds eight |

A sixth finding is smaller and real. The inventory endpoint names the device
state `connected` and carries a boolean. The table reads `status`, so all eight
devices read `unknown` while all eight were connected. `with_status_word` now
turns the boolean into a word, and the page reads `connected` for each one.

`tests/unit/upgrade_portal/test_privilege_shapes.py` holds 15 tests that copy the
shapes the library and the device module really answer with. Every one of the six
findings fails at least one of those tests against the old code.

---

## 4. The one step that still fails, and why

The capture wrote its backup file and did not verify. `capture/store.py` sets
`verified` only when the database holds the document and the read-back matches.
The portal ran with `MISTHELPER_STANDALONE=true`, which is the workaround that
`HANDOFF.md` section 4.2 names for issue
[#1824](https://github.com/jmorrison-juniper/MistHelper/issues/1824). That issue
records that `DatabaseRouter` drops every ArangoDB write outside a container.

The page therefore reads "The portal could not store the capture", which is
true and correct. The read half of the capture is proved. The store half needs
either the container or the fix of issue #1824, and this record does not count
it as a pass.

---

## 5. What stays open

| Item | Why |
| --- | --- |
| The comparison of two captures, scenario B | It needs two stored captures, and the store does not verify here |
| The upgrade and the settle gate, scenario C | It writes firmware to a real device. A person must decide to run it |
| The stop control, scenario D | It needs a live upgrade |
| T233, the container proof | Section 4 shows why the container is the environment that closes both this and the store |

Scenario E, the two operators, is covered by the browser suite, which now drives
all six lock controls of the page. Scenario F, the history view, is covered the
same way.

---

## 6. How to repeat this run

```powershell
$env:MISTHELPER_STANDALONE = "true"
$env:REDIS_HOST = "127.0.0.1"
$env:REDIS_PASSWORD = "misthelper"
.\.venv\Scripts\python.exe -m waitress --listen=127.0.0.1:8056 --threads=4 wsgi_capture:app
```

Open `http://127.0.0.1:8056/`, choose the token mode, and type any work address.
The token names the organization, so the address only labels the log record.
