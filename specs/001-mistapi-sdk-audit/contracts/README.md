# Contracts

This audit-only feature does not introduce a new external interface contract.

The planned work is limited to internal compatibility review and the follow-up MistHelper.py call-site updates required to stay compatible with newer MistAPI releases.

If a later implementation changes public CLI behavior, menu behavior, or output schemas, add those contracts here.
