# Audit: Menu 96 - Export gateways with duplicate WAN IPs

Target: GatewayStatsExporter.wan_port_conflicts

Purpose: Verify completeness, test coverage, and SQL export compliance (ENDPOINT_PRIMARY_KEY_STRATEGIES + DataExporter usage).