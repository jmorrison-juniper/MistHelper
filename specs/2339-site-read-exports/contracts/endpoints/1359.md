<!-- site-read-handoff:packet-07-issue-1359 -->
# Endpoint packet 7 of 20: #1359 - Site WxLAN tunnels

**Original issue:** #1359.
**Original specification:** `specs/851-mist-list-site-wx-tunnels/spec.md`.
**New contract file:** `specs/2339-site-read-exports/contracts/endpoints/1359.md`.
**Status:** The packet is planned. None of its implementation tasks ran.
**Dependency:** Complete T066, which completes the prior packet before this packet.

## Specification overlay

Read site-level tunnel definitions safely.

This packet inherits US1-US4 and FR-001 through FR-016 from the shared specification. It adds the exact endpoint binding and the unique tests below. The existing spec's requirement for a separate top-level menu is replaced by the single family menu in #2339.

The original FR-001 maps to shared FR-001/FR-003. Original FR-002 maps to shared FR-002/FR-006. Original FR-003 maps to shared FR-004/FR-005. Original FR-004 and FR-005 map to shared FR-009/FR-011/FR-012. Original FR-006 and FR-007 map to shared FR-008/FR-014. Original FR-008 maps to shared FR-016. No read-only or persistence requirement is dropped.

## Verified source and SDK contract

- OperationId: `listSiteWxTunnels`.
- HTTP method: GET.
- Exact path: `/api/v1/sites/{site_id}/wxtunnels`.
- SDK module: `mistapi.api.v1.sites.wxtunnels`.
- SDK source: `.venv/Lib/site-packages/mistapi/api/v1/sites/wxtunnels.py` at the `listSiteWxTunnels` definition. The audited definition starts at line 17.
- Parsed signature: `mist_session: _APISession, site_id: str, limit: int | None=None, page: int | None=None`.
- Response: The committed OpenAPI declares an array. Require a list of dictionaries at runtime.
- Catalog mode: paginated=True. Pass limit=runtime.page_limit and page=1. Keep default_kwargs empty.
- Existing strategy: `natural_pk` with primary_key `[id]`.
- Opt-in physical storage fields: `[site_id, id]`.
- Primary output basename: `SiteRead_listSiteWxTunnels_` followed by the canonical site UUID without hyphens, then `.csv`.

The SDK's normal import is not proven by this packet. The planning environment had a broken simplejson dependency. The isolated existing-function probe accepted the planned arguments and issued one fake GET without a network call.

## Where to find the contract

Read the source spec first. In `documentation/mist-api-openapi3json.json`, locate the exact operationId under the path above. Follow local component references for the 200-response item shape. Read the installed SDK definition next. The SDK filename, not the URL's trailing segments, determines the module.

Read the matching dictionary entry in `src/refactors/endpoint_primary_key_strategies.py`. Metadata and graph mappings are not callable exporter integration. On the audited remote commit, no application export path referenced this selected callable. Recheck that fact before changing current main.

## Data model and acceptance fixtures

Two rows with distinct id values, name, peer fields, and a nested secret sentinel.

Use deterministic fake UUIDs, MAC addresses, and plain names. Do not paste production payloads or credentials into tests. For credential cases, use distinct sentinels such as `TEST_SECRET_1359_NOT_REAL` and assert they disappear. A synthetic test fixture id is allowed. Inventing an id in production normalization is not.

For every valid fixture, assert that the original response remains unchanged. Assert that the output includes selected org_id and site_id. For derived rows, preserve a different original site as source_site_id under the shared scope contract.

## Endpoint-specific checks

Assert tunnel secrets disappear from flattened rows, safe raw_data, and captured logs. Preserve peer identifiers. Do not call createSiteWxTunnel, updateSiteWxTunnel, or any connectivity operation.

Also assert all of these outcomes:

1. The catalog resolves the exact callable and supplies only supported arguments.
2. A successful nonempty array reaches the canonical writer once, after complete fetch validation.
3. A 200 empty list produces an empty outcome and no writer call.
4. A 401, 403, 404, 429, 500, missing status, dictionary error body, or mixed scalar row produces an error and no writer call.
5. A declared continuation is followed through the same fake session. An invalid, repeated, or failed continuation produces no export.
6. A missing required business-key component fails before persistence. Do not add a fallback id.
7. The same records written twice keep the expected row count. Changing a non-key value updates it.
8. The same business identity at a second site produces a distinct endpoint-document key. Every component in [id] matters.
9. Credentials are absent from flattened rows, raw_data, and captured logs. Noncredential nested data remains available.
10. A false primary writer result produces an error, not a saved message.

## Exact edit sequence

- [ ] T067 [US1] Add the #1359 case to tests/unit/export/site_read/conftest.py and test_catalog.py. Verify listSiteWxTunnels, its SDK signature, and its exact GET path. Map: FR-001, FR-003.
- [ ] T068 [US1] Add only the #1359 metadata row to src/export/site_read/catalog.py. Use mistapi.api.v1.sites.wxtunnels and the packet keyword mode. Map: FR-001, FR-003, FR-004.
- [ ] T069 [US3] Add storage_key_fields [site_id, id] to the existing listSiteWxTunnels strategy in src/refactors/endpoint_primary_key_strategies.py. Keep type and primary_key unchanged. Map: FR-011, FR-012.
- [ ] T070 [US2] Add #1359 happy, empty, malformed, cancelled, and failed-page cases to test_fetch.py and test_service.py. Assert exact rows and no partial write. Map: FR-004, FR-006, FR-007.
- [ ] T071 [US3] Add #1359 repeat-write, site-separation, key-component, and redaction assertions to test_storage.py. Apply the packet's unique checks. Map: FR-008, FR-009, FR-010, FR-011, FR-013.
- [ ] T072 [US4] Correct specs/851-mist-list-site-wx-tunnels/spec.md for the verified SDK module and family menu. Run the #1359 focused cases and the shared suite. Record evidence before marking this packet complete. Map: FR-014, FR-015, FR-016.

## Focused commands and expected results

From the implementation worktree, run `rtk proxy .venv\Scripts\python.exe -m pytest tests/unit/export/site_read/ -k issue_1359 -q --timeout=120`.

Give the parameterized cases the id `issue_1359` so this selector collects real tests. Require at least one catalog, fetch, service, and storage case. A zero-test selection is a failure, not proof of success.

Then run the whole `tests/unit/export/site_read/` suite. Require zero failed tests and no unexpected skips. Run the shared writer and redactor regressions after any shared-code change. Record actual counts and exit codes in the feature verification record.

## Completion and stop conditions

Complete this packet only after its cases pass, its source spec is corrected, and the shared contracts remain true. Menu discoverability is accepted at the final integration phase, not by leaving an unused class in src.

If the SDK function disappears, current main gains an implementation, another agent claims the issue, or real identity differs from this contract, stop and report the evidence. Do not guess, overwrite other work, add a suppression, or silently change the issue scope.
