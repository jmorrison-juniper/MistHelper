# Backend API URL (only used in development — Nginx proxies in production)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Polling intervals (milliseconds)
VITE_POLLING_ACTIVE_MS=5000
VITE_POLLING_PASSIVE_MS=30000
